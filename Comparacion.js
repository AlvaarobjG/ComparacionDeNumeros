function pedirNumeros(cantidadDenumeros){
    const numeros=[];

    for (let i = 0; i < indice; i++) {
       const numeroDado = Number(prompt('Ingresa el numero; ${i+1}'));
    }
    return numeros;
};

function ordenar()